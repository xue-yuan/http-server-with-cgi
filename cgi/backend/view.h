#ifndef VIEW_H
#define VIEW_H

#include "lib/utils.h"

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char *self_index(Parameter *);
char *upload(Parameter *);
char *view(Parameter *);

#endif
